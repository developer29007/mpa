{
  "permissions": {
    "allow": [
      "Bash([ -f .env ] && set -a && source .env && set +a*)",
      "Bash(set -a && source .env && set +a*)",
      "Bash(PYTHONPATH=src pdm run python -m itch.itch_runner *)",
      "Bash(PYTHONPATH=src pdm run python -m consumers.db_consumer *)",
      "Bash(git pull*)",
      "Bash(git push*)",
      "Bash(git add *)",
      "Bash(git commit*)"
    ]
  }
}
